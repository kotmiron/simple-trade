{% extends 'SimpleTradeBundle::base.html.twig' %}

{% block body %}
	Список шаблонов:

	<form action="{{ path('templates_create') }}" method="post" {{ form_enctype(form) }}>
		{{ form_widget(form) }}
		<p>
			<button type="submit">Создать</button>
		</p>
	</form>

	<ul class="record_actions">
		<li>
			<a href="{{ path('templates') }}">
				Вернуться к списку
			</a>
		</li>
	</ul>
{% endblock %}
